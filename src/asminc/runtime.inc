; Definitions for runtime used in code generated by the Pascal compiler

zp_base := $02

sp := zp_base + $00
sreg := zp_base + $02
ptr1 := zp_base + $04
ptr2 := zp_base + $06
ptr3 := zp_base + $08
ptr4 := zp_base + $0a
tmp1 := zp_base + $0c
tmp2 := zp_base + $0d
tmp3 := zp_base + $0e
tmp4 := zp_base + $0f
intOp1 := zp_base + $10
intOp2 := zp_base + $12
intPtr := zp_base + $14
stackP := zp_base + $16
savedStackPtr := zp_base + $18
currentNestingLevel := zp_base + $19
